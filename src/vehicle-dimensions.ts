// Shared visual and collision scale: 22% smaller than the original APEX 07.
export const VEHICLE_SCALE = .78;
export const CONTACT_RADIUS = VEHICLE_SCALE;
export const CONTACT_AXLE = .9 * VEHICLE_SCALE;
