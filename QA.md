# 第一版验证记录

- 构建：`npm run build`，TypeScript 与 Vite 通过。
- 浏览器实际操作：点击开始，经过倒计时，按 ↑ 前进；车辆位置移动，速度约 15.79 m/s，成功收集薯币。
- 方向映射：ArrowLeft 输入 -1、ArrowRight 输入 +1。相同初始条件下，右转半秒航向变化 -0.54946 rad，左转 +0.54946 rad；符合车辆 +Z 前向的驾驶方向。
- 加速、刹车减速、倒车、漂移状态：通过固定步长模拟。
- 氮气消耗：通过；低电量持续按住加速 3 秒仅 3 次状态切换，避免逐帧来回闪烁。
- 跳台：发生离地，最高车身基准高度 1.755 m，随后落地。
- 碰撞：车辆与圆形障碍分离，触发碰撞反馈。
- 计圈：跳过检查点跨终点不计圈；顺序走过 8 个检查点，3 圈后完成。该项用模拟位置验证比赛状态机，不等于 AI 自动驾驶了完整三圈。
- 资源：使用本地 GLB、本地字体与 favicon，不需要第三方运行时资源请求。
- 画面：检查前后修改后的路肩、车体、阴影、桌面驾驶 HUD；390×844 移动视口检查。
- 固定构建：通过 HTTP 预览运行，避免开发 HMR 打断试玩。

场景合批后，开场总览的绘制调用从约 719 降至约 245；最终数字随相机、可见物和粒子变化。这不是跨设备 FPS 保证。

没有进行真实手机、Safari 全版本或长时间联机压力测试（本版没有联机）。

最终固定构建检查：390×844 视口无横向溢出，6 个触屏按钮均位于屏幕内；按住加速按钮 0.9 秒后 HUD 显示 38 km/h；生产环境无开发调试接口，浏览器控制台 0 错误、0 警告。

## 2026-09-10 四车模式

- 三辆 AI 在固定 60 Hz 实际车辆模拟中完成三圈：皮皮薯 46.50 秒、萝卜头 50.85 秒、豆不急 55.90 秒。未通过传送检查点完成比赛，恢复次数均为 0。玩家在赛道外静止，结果不代表有人持续阻挡时的用时。
- 连续模拟 150 秒，完成后的计时不再增长；AI 不覆盖玩家本地最佳圈速。
- 暂停时所有 AI 坐标保持不变；单车模式电脑车不更新并隐藏。
- 四车模式重启恢复发车位、检查点与计时。
- 对称碰撞检查覆盖同向追尾、速度交换和车身分离；实时排名受有效检查点约束，跳关不能取得领先。
- 三车共享原模型几何，车漆材质独立复制；保留深度冲突修复。

2026-09-10：AI 车辆 3 圈模拟通过；暂停、单车练习切换、实时排名与完赛停车通过。

## 四款怪车与尺寸调整

- 四款玩家车型逐一加速通过：歪嘴小钢炮、暴走小土豆、卷王铅笔、迷路飞碟；每次只显示所选车型，其他三款作为对手。
- 统一模型缩放 0.78；四款模型宽度约 1.35–1.88 m、长度约 2.41–3.25 m。碰撞半径 0.78 m，前后圆心偏移 0.702 m；追尾测试分离距离 2.964 m，双方速度响应正常。
- 调整后 AI 实际模拟三圈用时约 46.50 / 50.85 / 55.90 秒，恢复次数均为 0。该场景玩家停在赛道外，不代表持续阻挡下的表现。
- 回归检查右转航向、暂停冻结飞碟动画、刷新保留选车通过。
- Playwright 实际点击四张车型卡、确认选车、开始、键盘加速、暂停返回车库通过。
- 390×844 手机模拟：车库双列、四张卡片无横向溢出；六个触屏按钮均在屏内；按住加速 0.9 秒达到约 38 km/h，飞碟悬浮位置随时间变化。
- 检查了 1440×900 桌面、390×844 手机以及 844×390 横屏截图。横屏车库内容可滚动，确认按钮可正常点击。
- 四张模型预览经 Canvas 像素检查：非透明像素约 1.47–3.95 万，颜色分组 159–284，均非空白。截图与实际赛道中的模型轮廓一致。
- 起点横梁在遮挡跟车视线时变透明，驾驶截图确认能看清玩家车辆。
- 浏览器交互检查未捕获页面脚本错误。真实手机 GPU 尚未实测。
- 最终生产构建通过；4173 固定版本实际选铅笔车、刷新、驾驶和返回车库通过，按油门约 1 秒 HUD 显示 41 km/h，开发调试接口不存在。
- 生产版桌面与手机主画布各采样 160×100 像素，均为 16000 个非透明像素，颜色分组分别为 148 / 229，确认场景非空白。

## 2026-09-10 局域网真人同场

- `npm run build`：前端 TypeScript、服务器 TypeScript、Vite 生产构建通过。Vite 的单包体积提示仍存在，主包约 674 kB / gzip 183 kB。
- `npm run test:network`：5 项测试全部通过。覆盖房间容量、准备条件、房主发车权限、统一倒计时、同场碰撞、过期输入释放、独立薯币、检查点顺序、完赛计时、重赛重置、房主转移、退出标记和比赛时限。
- 完整三圈使用测试驾驶控制器持续向共享服务器模拟输入转向与油门，未传送检查点；两人用时约 47.63 / 55.43 秒。之后验证完赛时间冻结、回到候场和第二局重置。测试控制器只存在于测试文件，真人房间没有 AI 补位。
- 5 个独立 WebSocket 连接测试：4 人满员、拒绝第 5 人、错误房间码、非房主发车、未全员准备、非法位置/成绩消息无效；一人输入油门后另一连接收到其位移；房主退出后迁移；全员退出清理房间。
- Playwright 使用两个独立浏览器上下文（桌面 1440×900、手机模拟 390×844），经 UI 建房、邀请链接加入、选车、准备和发车。两边相同服务器 tick 的完整状态 JSON 一致；桌面玩家油门输入后，朋友收到约 8.46 m 位移。
- 手机触屏按钮的指针按下/释放传到服务器，另一端看到飞碟加速。联机画布采样 160×100 像素，颜色分组 192，非空白；手机页面宽 390 / scrollWidth 390，房间无横向溢出。保存并目视检查了两端候场与比赛截图。
- 浏览器测试验证退出联机后的本地驾驶恢复，以及房主退出后的在线玩家接任；未捕获页面脚本错误。
- 初次验证遇到本机严重负载导致测试超时，关闭本任务旧浏览器后单独重跑通过；上述是最终通过结果，不是跨设备性能保证。
- 切换到最终局域网地址 `http://10.15.39.238:4173/` 后，双浏览器完整流程再次通过：共同 tick 620 的状态一致、对端观测位移约 8.01 m、移动画布颜色分组 193、页面脚本错误 0；全员退出后 `/health` 显示 rooms=0。

实际验证为本机上的独立客户端与浏览器手机视口，尚未用两台物理设备或真实手机 GPU 验证。当前针对同一局域网；不支持公网配对、断线恢复本局或高延迟客户端预测。重启服务器会清空房间。
